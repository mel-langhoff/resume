require 'rails_helper'

RSpec.describe 'Root Page', type: :feature do
  describe 'when a user visits the root path /' do
    it "has links to about, resume, portfolio, contact" do

    end

    it "has a link to vinyl collection" do

    end
  end
end